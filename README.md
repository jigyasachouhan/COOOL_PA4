# COOOL_PA4
Code Optimization for Object-Oriented Languages


Usage:

To compile tc and analyse and generate unoptimized and optimized class files:
make clean && bash compile.sh && bash run.sh Test1 > output.txt

To run unoptimized and optimized test files and compare:
bash runtest.sh

Clean repo:
make clean